{
  "name": "cli_v2",
  "version": "1.0.0",
  "description": "cli tool for the react native typescript template",
  "main": "src/app.ts",
  "author": "areeba",
  "license": "MIT",
  "private": true,
  "scripts": {
    "start": "yarn build && yarn run:build",
    "run:build": "node dist/app.js",
    "build": "tsc -p .",
    "init": "yarn install && yarn build"
  },
  "dependencies": {
    "chalk": "^4.1.0",
    "cli-color": "^2.0.0",
    "figlet": "^1.5.0",
    "inquirer": "^7.3.3",
    "typescript": "^4.0.3"
  },
  "devDependencies": {
    "@types/cli-color": "^2.0.0",
    "@types/figlet": "^1.2.0",
    "@types/inquirer": "^7.3.0"
  }
}
