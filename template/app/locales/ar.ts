import { reduxNameSpace } from '&features/redux/reduxi.i18n';
import { localStorageNameSpace } from '&features/localStorage/localStorage.i18n';
import { landingNameSpace } from '&features/landing/landing.i18n';
import { apiNameSpace } from '&features/api/api.i18n';

let ar = {
  /* [TOKEN]:LOCALS -- Do not remove */
  landing: landingNameSpace.ar,
  redux: reduxNameSpace.ar,
  localStorage: localStorageNameSpace.ar,
  api: apiNameSpace.ar,
};

export default ar;
